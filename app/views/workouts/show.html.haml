%p= @workout.date.strftime("%m/%d/%Y at %I:%M%p ")
%p= @workout.workout
%p= @workout.mood
%p= @workout.length

= link_to "Back", root_path
= link_to "Edit", edit_workout_path
= link_to "Delete", @workout, method: :delete, data: {confirm: "are you sure?"}